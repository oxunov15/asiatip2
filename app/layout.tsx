import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "AsiaTip - Ultimate Donation Platform for Central Asian Creators",
  description:
    "Supercharge your creator journey with real-time tips, overlay widgets, and analytics. Built for streamers and content creators across Central Asia.",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
